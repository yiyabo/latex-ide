[package]
name = "yiyabo-desktop"
version = "0.1.0"
description = "yiyabo — AI LaTeX writing workbench (desktop shell)"
authors = ["latex-ide"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "yiyabo_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "yiyabo"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
